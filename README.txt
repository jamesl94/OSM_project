Report is Report.md

Run data.py to create cleaned .csv files then csv_to_sql.py to create database. OSM_PATH in data.py must be changed if running directory downloaded from Github, original OSM file is too large to upload. 